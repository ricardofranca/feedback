/*import React, { PropTypes } from 'react';
import { connect } from 'react-redux';
import { authDispatchers } from 'api/actions';

class Step4Feedback extends React.Component {

    // onChange = ({ target: { name, value } }) => {
    //     const fn = this.props.onChange({
    //         [name]: value,
    //     });
    // }

    render() {
        

        return (
            <div>
                <h1>asdf</h1>
            </div>
        );
    }
}

//export default connect(({ auth }) => ({ auth }), authDispatchers)(Step4Feedback);
export default connect (null, authDispatchers) (Step4Feedback);*/
